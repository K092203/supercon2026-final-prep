// solver_naive.cpp — 愚直解テンプレート
// 役割: stress.py の "naive" ターゲット。build/fast (skeleton) と出力を比較する。
// 本選当日に O(N²) 参照実装をここに書いて「fast が正しいか」を確認する。
#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // CUSTOMIZE: 愚直解をここに実装する
    return 0;
}
